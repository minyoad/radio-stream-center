services:
  radio-manager:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: radio-channel-manager
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
